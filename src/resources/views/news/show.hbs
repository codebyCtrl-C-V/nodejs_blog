<div class="mt-4">
    <div class="row">
        <div class="col-lg-3">
            <h3>Đăng ngày: {{createdAt}}</h3>
            <h3>Cập nhật lần cuối: {{updatedAt}}</h3>
        </div>
        <div class="col-lg-9">
            <h1>{{title}}</h1>
            <img src="{{image}}" alt="Ảnh tin tức" width="800" height="auto">

            <p>{{content}}</p>
        </div>
    </div>
</div>